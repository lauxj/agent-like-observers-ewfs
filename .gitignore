.idea/
.venv/
.DS_Store
__pycache__/
*.py[cod]

# Keep only the selected paper/reproducibility data in Git.
data/*

!data/paper_data/
!data/paper_data/**

results/

# Keep macOS Finder metadata ignored even inside unignored data folders.
**/.DS_Store

# Local-only connectivity plotting work; not pushed until it is ready again.
scripts/plot_ibm_connectivity.py
src/ewfs/analysis/plot_ibm_connectivity.py

# Local-only time-ordering work; not pushed until it is ready again.
scripts/plot_time_ordering.py
src/ewfs/analysis/time_ordering_hardware.py
